# Check that data-raw outputs are up to date.
#
# Runs data-raw/rebuild.R and fails if any tracked files changed,
# meaning the committed outputs are stale.

name: Data Raw Check

on:
  workflow_dispatch:
    inputs:
      r-version:
        description: "The version of R to use"
        default: "release"
        required: false
        type: choice
        options:
          - devel
          - latest
  workflow_call:
    inputs:
      r-version:
        description: "The version of R to use"
        default: "release"
        required: false
        type: string

concurrency:
  group: data-raw-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  data-raw-check:
    name: Data Raw
    runs-on: ubuntu-latest
    if: >
      !contains(github.event.commits[0].message, '[skip data-raw]')
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup R
        uses: r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6 # v2
        with:
          r-version: ${{ inputs.r-version }}
          use-public-rspm: true

      - name: Install R Dependencies
        uses: r-lib/actions/setup-r-dependencies@d3c5be51b12e724e68f33216ca3c148b66d5f0b6 # v2

      - name: Run data-raw/rebuild.R
        run: source("data-raw/rebuild.R")
        shell: Rscript {0}

      - name: Check non-data outputs are up to date
        run: |
          # Check text-based outputs (CSS, test CSVs) via git diff.
          # data/*.rda is excluded — binary serialization is not
          # deterministic across R versions/platforms. Data staleness
          # is handled by rebuild.R's in-memory comparison above.
          git status -s -- ':!data/*.rda'
          if [[ -n $(git status -s -- ':!data/*.rda') ]]; then
            echo "::error::Non-data outputs are stale. Run 'Rscript data-raw/rebuild.R' and commit the results."
            echo ""
            echo "The following files differ:"
            git diff --stat -- ':!data/*.rda'
            echo ""
            git diff -- ':!data/*.rda'
            exit 1
          else
            echo "All outputs are up to date."
          fi
        shell: bash
