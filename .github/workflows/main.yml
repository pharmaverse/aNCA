# Source: https://github.com/pharmaverse/admiralci
# Adapted from admiralci workflows. Runs on ubuntu-latest
# with r-lib/actions for R setup and dependency management.
#
# Last workflow update: 2026-05-04
name: admiral CI/CD Workflows

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: admiral-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  R_VERSION: "release"

jobs:
  get_r_version:
    name: Get R version
    runs-on: ubuntu-latest
    outputs:
      r-version: ${{ steps.get_r_version.outputs.R_VERSION }}
    steps:
      - name: Get R Version for Downstream Jobs
        id: get_r_version
        run: echo "R_VERSION=$R_VERSION" >> $GITHUB_OUTPUT
        shell: bash

  spellcheck:
    name: Spelling
    uses: ./.github/workflows/spellcheck.yml
    needs: get_r_version
    with:
      r-version: "${{ needs.get_r_version.outputs.r-version }}"

  linter:
    name: Lint
    uses: ./.github/workflows/lintr.yml
    needs: get_r_version
    with:
      r-version: "${{ needs.get_r_version.outputs.r-version }}"

  man-pages:
    name: Man Pages
    uses: ./.github/workflows/man-pages.yml
    needs: get_r_version
    with:
      r-version: "${{ needs.get_r_version.outputs.r-version }}"
    
  tests:
    name: Tests
    uses: ./.github/workflows/test.yml
    needs: get_r_version
    with:
      r-version: "${{ needs.get_r_version.outputs.r-version }}"

  check:
    name: Check
    uses: ./.github/workflows/r-cmd-check.yml
    with:
        error-on: note
