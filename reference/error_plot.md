# Create a simple error plot with a message

This internal function generates a minimal ggplot2 plot displaying a
given error message. It is used to return a plot object with a custom
error message.

## Usage

``` r
error_plot(msg)
```

## Arguments

- msg:

  Character string. The error message to display in the plot.

## Value

A ggplot object with the error message displayed.
