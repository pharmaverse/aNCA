# Strip the "(NA)" sentinel from a filter value vector

Strip the "(NA)" sentinel from a filter value vector

## Usage

``` r
strip_na_sentinel(value)
```

## Arguments

- value:

  Character vector of filter values.

## Value

A list with `value` (cleaned vector) and `include_na` (logical).
