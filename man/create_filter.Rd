% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_filter.R
\name{create_filter}
\alias{create_filter}
\title{Create a Filter UI Component}
\usage{
create_filter(filter_id, dataset)
}
\arguments{
\item{filter_id}{A unique identifier for the filter.}

\item{dataset}{A data frame containing the dataset to be filtered.}
}
\value{
A Shiny UI component for creating a filter.
}
\description{
This function generates a UI component for creating a filter in a Shiny application.
The filter allows users to select a column, condition, and value to filter a dataset.
}
\details{
The function creates a UI component with the following elements:
\itemize{
\item A horizontal rule (\verb{<hr>}).
\item A dropdown menu for selecting a column from the dataset.
\item A dropdown menu for selecting a condition (e.g., \code{==}, \code{>}, \code{<}, \code{>=}, \code{<=}).
\item A text input for specifying the filter value.
\item A submit button to confirm the filter.
\item A remove button to remove the filter.
}
}
\examples{
\dontrun{
  # Example usage in a Shiny app:
  ui <- fluidPage(
    create_filter("filter1", mtcars)
  )
  server <- function(input, output, session) {}
  shinyApp(ui, server)
}

}
