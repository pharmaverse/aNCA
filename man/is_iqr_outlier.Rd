% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_pp_outliers.R
\name{is_iqr_outlier}
\alias{is_iqr_outlier}
\title{Flag Tukey/IQR outliers within groups}
\usage{
is_iqr_outlier(data, value_col, group_cols = character(0))
}
\arguments{
\item{data}{A data frame.}

\item{value_col}{Name of the numeric column to test.}

\item{group_cols}{Character vector of columns defining comparison groups.
When empty or \code{NULL}, all rows form a single group. Columns not present in
\code{data} are ignored.}
}
\value{
A logical vector, one element per row of \code{data}, \code{TRUE} where the
value is a Tukey/IQR outlier within its group. \code{NA} values are \code{FALSE}.
Groups with fewer than 4 non-\code{NA} values yield all \code{FALSE} (an IQR is not
meaningful for very small samples).
}
\description{
Marks values that fall below \code{Q1 - 1.5 * IQR} or above \code{Q3 + 1.5 * IQR}
within each group defined by \code{group_cols}. This is the same rule the
boxplot whiskers use, so a flagged value corresponds to a point drawn
beyond a whisker.
}
\examples{
d <- data.frame(
  g = rep(c("a", "b"), each = 5),
  v = c(1, 2, 3, 4, 100, 10, 11, 12, 13, 14)
)
is_iqr_outlier(d, "v", "g")
}
