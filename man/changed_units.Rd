% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conversions.R
\name{changed_units}
\alias{changed_units}
\title{Keep only units that differ from their data-derived default}
\usage{
changed_units(units)
}
\arguments{
\item{units}{A units table with \code{PPORRESU} and \code{PPSTRESU} columns.}
}
\value{
The subset of rows where \code{PPSTRESU} differs from \code{PPORRESU}.
Rows where either unit is \code{NA} are excluded to avoid emitting
\code{NA}-based entries downstream.
}
\description{
Change detection is value-based: a row is considered "changed" when its
target unit (\code{PPSTRESU}) differs from the original unit (\code{PPORRESU}). This
replaces the previous \code{default} flag, which only tracked edits made through
the Units modal and missed automatic changes such as volume simplification.
}
