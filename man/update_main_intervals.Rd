% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals.R
\name{update_main_intervals}
\alias{update_main_intervals}
\title{Update an intervals data frame with user-selected parameters by study type}
\usage{
update_main_intervals(
  data,
  parameter_selections = NULL,
  int_parameters = NULL,
  start_impute = TRUE,
  blq_imputation_rule = NULL
)
}
\arguments{
\item{data}{A PKNCAdata object containing intervals and dosing data.}

\item{parameter_selections}{A named list of selected PKNCA parameters by study type.}

\item{int_parameters}{A data frame containing partial AUC ranges. In addition to
the required \code{parameter}, \code{start_auc} and \code{end_auc} columns, two optional columns
are honoured when present:
\itemize{
\item \code{custom_name}: a user-defined label for the interval. When non-empty it replaces
the standard \code{PPTESTCD_<start>-<end>} name in user-facing outputs. Carried onto the
generated interval rows as the \code{interval_name} column.
\item \code{study_type}: restricts the interval to a single study type (as produced by
\code{\link[=detect_study_types]{detect_study_types()}}). \code{NA}, an empty string, or \code{"All"} applies the interval to
every study type (the default).
}}

\item{start_impute}{Logical indicating whether to impute start values for
parameters. Named \code{start_impute} (not \code{impute}) to avoid colliding with the
per-interval \code{impute} column referenced under dplyr data-masking below.}

\item{blq_imputation_rule}{A list defining the Below Limit of Quantification (BLQ)
imputation rule using PKNCA format. The list should either contain three elements named:
\code{first}, \code{middle}, and \code{last} or two elements named \code{before.tmax} and \code{after.tmax}.
Each element can be a numeric value (substituting the BLQ value), or a string such as
\code{"drop"} (ignores the value) or \code{"keep"} (keeps the value as 0). Default is NULL,
which does not specify any BLQ imputation in any interval.}
}
\value{
An updated PKNCAdata object with parameter intervals based on user selections.
}
\description{
Update an intervals data frame with user-selected parameters by study type
}
