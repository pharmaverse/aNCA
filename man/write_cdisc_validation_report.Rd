% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc_validation_report.R
\name{write_cdisc_validation_report}
\alias{write_cdisc_validation_report}
\title{Validate CDISC data and write an HTML QC report for the export flow}
\usage{
write_cdisc_validation_report(
  cdisc_data,
  target_dir,
  filename = "cdisc_validation_report.html",
  project = NULL,
  metadata = metadata_nca_variables
)
}
\arguments{
\item{cdisc_data}{Named list of CDISC data frames keyed \code{pp}, \code{adpp},
and/or \code{adnca} (as returned by \code{\link[=export_cdisc]{export_cdisc()}}).}

\item{target_dir}{Directory the report is written into. Created if it does
not exist.}

\item{filename}{File name for the report. Defaults to
\code{"cdisc_validation_report.html"}.}

\item{project}{Optional project name shown in the report. Defaults to
\code{NULL}.}

\item{metadata}{Metadata data frame passed to \code{\link[=validate_cdisc_types]{validate_cdisc_types()}}.
Defaults to \code{metadata_nca_variables}.}
}
\value{
A list with elements \code{findings} (the findings data frame),
\code{report_path} (path to the written HTML report), and \code{blocks_save}
(logical from \code{\link[=cdisc_validation_blocks_save]{cdisc_validation_blocks_save()}}).
}
\description{
Convenience wrapper for the export/Save flow: runs
\code{\link[=validate_cdisc_types]{validate_cdisc_types()}}, writes the HTML report from
\code{\link[=cdisc_validation_report]{cdisc_validation_report()}} into \code{target_dir}, and returns the findings
alongside the report path and a save-blocking flag.
}
\examples{
cdisc_data <- list(
  adnca = data.frame(STUDYID = "S1", AVAL = 1.2, stringsAsFactors = FALSE)
)
res <- write_cdisc_validation_report(cdisc_data, target_dir = tempdir())
res$blocks_save
}
