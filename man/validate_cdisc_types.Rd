% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_cdisc_types.R
\name{validate_cdisc_types}
\alias{validate_cdisc_types}
\title{Validate CDISC datasets against metadata-declared data types}
\usage{
validate_cdisc_types(
  cdisc_data,
  metadata = metadata_nca_variables,
  check_length = TRUE
)
}
\arguments{
\item{cdisc_data}{Named list of CDISC data frames keyed \code{pp}, \code{adpp},
and/or \code{adnca} (as returned by \code{\link[=export_cdisc]{export_cdisc()}}). Entries with other
names or that are not data frames are ignored.}

\item{metadata}{Metadata data frame with \code{Dataset}, \code{Variable}, \code{Type},
and \code{Length} columns. Defaults to \code{metadata_nca_variables}.}

\item{check_length}{Logical. Check character values against the declared
\code{Length}? Defaults to \code{TRUE}.}
}
\value{
A tidy findings data frame with columns \code{Dataset}, \code{Variable},
\code{Check}, \code{Severity}, \code{Expected}, \code{Observed}, \code{N_Affected}, and
\code{Message}. Zero rows means the data conforms.
}
\description{
Checks each column of the supplied CDISC datasets against the expected
storage class and maximum length declared in \code{metadata_nca_variables}.
This is intentionally not a full CDISC conformance engine: it covers only
what the metadata can assert (class and length), and never fails on
metadata \code{Type} values it cannot interpret.
}
\details{
Indexed CDISC variable families are resolved to their metadata template: a
data column falls back from an exact name match to an index-agnostic match,
so \code{NCA1XRS}/\code{NCA2XRS} match the \code{NCAwXRS} template and \code{CRIT2}/\code{CRIT3FL}
match the \code{CRIT1}/\code{CRIT1FL} family. An exact metadata entry always takes
precedence over the template.
}
\examples{
cdisc_data <- list(
  adnca = data.frame(STUDYID = "S1", AVAL = 1.2, stringsAsFactors = FALSE)
)
validate_cdisc_types(cdisc_data)
}
