% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_cdisc_types.R
\name{describe_cdisc_variables}
\alias{describe_cdisc_variables}
\title{Describe CDISC dataset columns against the declared metadata}
\usage{
describe_cdisc_variables(
  cdisc_data,
  metadata = metadata_nca_variables,
  check_length = TRUE
)
}
\arguments{
\item{cdisc_data}{Named list of CDISC data frames keyed \code{pp}, \code{adpp},
and/or \code{adnca} (as returned by \code{\link[=export_cdisc]{export_cdisc()}}).}

\item{metadata}{Metadata data frame with \code{Dataset}, \code{Variable}, \code{Label},
\code{Type}, and \code{Length} columns. Defaults to \code{metadata_nca_variables}.}

\item{check_length}{Logical. Consider the declared \code{Length} when assigning
status? Defaults to \code{TRUE}.}
}
\value{
A data frame with one row per column of each dataset and columns
\code{Dataset}, \code{Variable}, \code{Label}, \code{Type}, \code{Expected_Class},
\code{Observed_Class}, \code{Max_Length}, \code{Longest_Value}, \code{N_NonMissing},
\code{Status}, and \code{Detail}. \code{Status} is one of \code{pass}, \code{error}, \code{unknown},
or \code{skipped}.
}
\description{
Produces a per-variable descriptive summary of the supplied CDISC datasets:
the declared label, type, expected and observed storage class, declared and
observed maximum length, and a conformance status for each column. This is
the descriptive counterpart to \code{\link[=validate_cdisc_types]{validate_cdisc_types()}}, which only returns
problems.
}
\examples{
cdisc_data <- list(
  adnca = data.frame(STUDYID = "S1", AVAL = 1.2, stringsAsFactors = FALSE)
)
describe_cdisc_variables(cdisc_data)
}
