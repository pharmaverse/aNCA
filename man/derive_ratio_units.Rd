% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio_calculations.R
\name{derive_ratio_units}
\alias{derive_ratio_units}
\title{Derive expected units for ratio parameters}
\usage{
derive_ratio_units(ratio_table, units_table)
}
\arguments{
\item{ratio_table}{Data frame with columns \code{TestParameter}, \code{RefParameter},
and \code{PPTESTCD} (as produced by the ratio calculations table in the app).}

\item{units_table}{Data frame with at least \code{PPTESTCD} and \code{PPORRESU}
columns (typically from \code{PKNCAdata$units}).}
}
\value{
A data frame with columns \code{PPTESTCD}, \code{PPORRESU}, \code{PPSTRESU},
\code{conversion_factor}, plus any group columns from \code{units_table} set to \code{NA}.
Returns \code{NULL} if \code{ratio_table} has zero rows.
}
\description{
For each row in \code{ratio_table}, looks up the original units of the test and
reference parameters in \code{units_table}, then determines the ratio unit:
\code{"fraction"} when the units are convertible, or a compound string via
\code{\link[=compose_ratio_unit]{compose_ratio_unit()}} otherwise.
}
\keyword{internal}
