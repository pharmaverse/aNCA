% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc_validation_report.R
\name{cdisc_validation_report}
\alias{cdisc_validation_report}
\title{Render CDISC validation findings as a self-contained HTML report}
\usage{
cdisc_validation_report(
  findings,
  datasets_checked = NULL,
  project = NULL,
  generated_at = Sys.time()
)
}
\arguments{
\item{findings}{A findings data frame as returned by
\code{\link[=validate_cdisc_types]{validate_cdisc_types()}}.}

\item{datasets_checked}{Optional character vector of dataset keys that were
checked, used for the report metadata line. Defaults to \code{NULL}.}

\item{project}{Optional project name shown in the report header. Defaults to
\code{NULL}.}

\item{generated_at}{Timestamp shown in the report. Defaults to
\code{Sys.time()}.}
}
\value{
A single character string containing a complete HTML document.
}
\description{
Produces a styled, standalone HTML string (inline CSS, no external assets)
summarising the findings from \code{\link[=validate_cdisc_types]{validate_cdisc_types()}}: a PASS/FAIL banner,
a per-finding table, and a footer noting the metadata-driven scope.
}
\examples{
findings <- validate_cdisc_types(list())
html <- cdisc_validation_report(findings)
}
