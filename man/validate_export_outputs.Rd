% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_export_outputs.R
\name{validate_export_outputs}
\alias{validate_export_outputs}
\title{Validate exported outputs before saving}
\usage{
validate_export_outputs(
  output,
  obj_names = NULL,
  metadata = metadata_nca_variables
)
}
\arguments{
\item{output}{The export list as assembled for the Save flow: a named,
possibly nested list of outputs (plots, tables, and \code{*_code} strings),
with the CDISC datasets under a \code{CDISC} element keyed \code{pp}/\code{adpp}/\code{adnca}.}

\item{obj_names}{Optional character vector of leaf names that will be
written. When \code{NULL} (the default) every leaf is validated.}

\item{metadata}{Metadata data frame passed to \code{\link[=validate_cdisc_types]{validate_cdisc_types()}} for
the CDISC value-level checks. Defaults to \code{metadata_nca_variables}.}
}
\value{
A tidy findings data frame with columns \code{Output}, \code{Variable},
\code{Check}, \code{Severity}, \code{Expected}, \code{Observed}, and \code{Message}. \code{Variable} is
\code{NA} for object-class findings and holds the column name for CDISC
value-level findings. Zero rows means every output is safe to export.
}
\description{
Checks every output that the export/Save flow would write, so a save can be
refused before any file is created. It combines two levels of checking:
}
\details{
\itemize{
\item \strong{Object class} for all outputs: plot nodes must be a ggplot or plotly,
table nodes must be a data frame, and \code{*_code} nodes must be a length-1
character string. Anything the export writer cannot serialise is reported
as an error.
\item \strong{Value level} for the CDISC datasets: the \code{pp}/\code{adpp}/\code{adnca} entries
under \code{CDISC} are additionally checked against the metadata-declared data
types and lengths via \code{\link[=validate_cdisc_types]{validate_cdisc_types()}}.
}

The walk mirrors the export writer: plain lists are recursed into, and when
\code{obj_names} is supplied only those named leaves (the ones that will actually
be written) are validated.
}
\examples{
output <- list(
  nca_results = list(nca_pkparam = data.frame(AVAL = 1.2)),
  CDISC = list(adnca = data.frame(STUDYID = "S1", AVAL = 1.2))
)
validate_export_outputs(output)
}
