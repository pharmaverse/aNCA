% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_pre_specs.R
\name{generate_pre_specs}
\alias{generate_pre_specs}
\title{Generate Pre-Specifications for CDISC Datasets}
\usage{
generate_pre_specs(datasets = c("ADNCA", "ADPP", "PP"), cdisc_data = NULL)
}
\arguments{
\item{datasets}{Character vector of dataset names to generate specs for.
Defaults to \code{c("ADNCA", "ADPP", "PP")}.}

\item{cdisc_data}{Optional named list of data frames (as returned by
\code{export_cdisc()}). Names should be lowercase (\code{pp}, \code{adpp}, \code{adnca}).
When provided, each spec is filtered to only variables present as columns
in the corresponding data frame.}
}
\value{
A named list of data frames, one per dataset, each containing
columns: Dataset, Order, Variable, Label, Type, Role, Core, Length.
}
\description{
Extracts variable-level metadata from \code{metadata_nca_variables} for the
requested CDISC datasets (ADNCA, ADPP, PP). When \code{cdisc_data} is provided,
specs are narrowed to only the variables present in each dataset.
}
